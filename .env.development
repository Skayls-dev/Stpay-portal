# Development environment variables
# Empty = use relative URLs, proxied by Vite (:3000 → :5169) — no CORS issues
VITE_API_BASE=
VITE_API_URL=
VITE_APP_NAME=ST Pay Gateway
VITE_APP_VERSION=1.0.0

# Demo credentials for testing
VITE_API_KEY=sk_test_local_stpay_2026

# Enable mock mode for client-side simulation
VITE_MOCK_MODE=false
VITE_MOCK_MODE_AUTO_APPROVE=true
